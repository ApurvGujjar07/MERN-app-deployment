apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: wanderlust
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2   # ✅ EKS default storage
  resources:
    requests:
      storage: 5Gi
